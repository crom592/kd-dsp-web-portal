# KD-DSP Web Portal

기업 관리자 및 KD 운영자를 위한 관리 웹 포털입니다.

## 개요

이 웹 포털은 두 가지 주요 사용자 그룹을 위한 통합 관리 인터페이스를 제공합니다:

1. **KD 플랫폼 운영자**: 전체 시스템 관제, 노선 설계, 데이터 분석
2. **기업 관리자**: 소속 기업의 통근버스 운영 현황 및 정산 관리

## 주요 기능

### KD 운영자 전용
- 🗺️ **실시간 관제 대시보드**: 모든 차량의 실시간 위치 및 상태 모니터링
- 🛣️ **노선 관리**: AI 기반 노선 설계 도구 활용
- 📊 **통합 데이터 분석**: 전체 운영 데이터 분석 및 리포트 생성
- 💰 **정산 관리**: 기업별 청구 데이터 생성 및 관리
- 👥 **사용자 관리**: 전체 사용자 및 권한 관리

### 기업 관리자 전용
- 👨‍💼 **임직원 관리**: 통근버스 이용 자격 부여 및 관리
- 📈 **탑승률 통계**: 소속 임직원의 탑승률 및 이용 패턴 분석
- 💳 **정산 내역**: 월별 이용 내역 및 청구 비용 확인
- 📝 **노선별 리포트**: 운행 효율 및 비용 분석 리포트

## 기술 스택

- **Framework**: React 18 + TypeScript
- **Routing**: React Router v6
- **State Management**: Redux Toolkit
- **UI Components**: Material-UI (MUI)
- **Data Visualization**: Recharts
- **Maps**: Kakao Maps API
- **API Client**: Axios + React Query
- **Form Management**: React Hook Form
- **Styling**: Emotion (CSS-in-JS)
- **Build Tool**: Vite

## 프로젝트 구조

```
src/
├── pages/                  # 페이지 컴포넌트
│   ├── Dashboard/         # 대시보드 (관제)
│   ├── Routes/            # 노선 관리
│   ├── Analytics/         # 데이터 분석
│   ├── Billing/           # 정산 관리
│   ├── Users/             # 사용자 관리
│   └── Login/             # 로그인
├── components/            # 재사용 가능한 컴포넌트
│   ├── common/           # 공통 컴포넌트 (Button, Input 등)
│   ├── layout/           # 레이아웃 컴포넌트 (Header, Sidebar)
│   ├── maps/             # 지도 관련 컴포넌트
│   └── charts/           # 차트 컴포넌트
├── services/             # API 통신 서비스
│   ├── api/             # API 클라이언트
│   ├── auth/            # 인증 서비스
│   └── monitoring/      # 실시간 데이터 서비스
├── store/                # Redux 스토어
│   ├── slices/          # Redux 슬라이스
│   └── hooks.ts         # 타입이 지정된 훅
├── utils/                # 유틸리티 함수
│   ├── date.ts          # 날짜 포맷팅
│   ├── format.ts        # 데이터 포맷팅
│   └── validation.ts    # 유효성 검사
├── hooks/                # 커스텀 훅
├── types/                # TypeScript 타입 정의
└── constants/            # 상수 정의
```

## 시작하기

### Prerequisites
```bash
Node.js >= 18.0.0
npm >= 9.0.0
```

### 설치
```bash
npm install
```

### 환경 변수 설정
`.env.example`을 복사하여 `.env` 파일을 생성합니다.

```bash
cp .env.example .env
```

필요한 환경 변수:
```env
VITE_API_BASE_URL=http://localhost:3000
VITE_KAKAO_MAP_API_KEY=your_kakao_map_api_key
VITE_WS_URL=ws://localhost:3000
```

### 개발 서버 실행
```bash
npm run dev
```

서버는 `http://localhost:5173`에서 실행됩니다.

### 빌드
```bash
npm run build
```

### 프리뷰
```bash
npm run preview
```

## 주요 화면

### 1. 실시간 관제 대시보드
- 전체 차량 실시간 위치 표시 (지도)
- 노선별 운행 현황
- 긴급 알림 및 이슈 현황
- 주요 KPI 요약 (가동률, 탑승률 등)

### 2. 노선 관리
- 노선 목록 조회 및 검색
- 신규 노선 생성 (AI 추천 포함)
- 노선 상세 정보 및 수정
- 노선 시뮬레이션

### 3. 데이터 분석
- 노선별 탑승률 통계
- 시간대별 수요 분석
- 운행 비용 분석
- 커스텀 리포트 생성

### 4. 정산 관리
- 기업별 월별 청구 내역
- 상세 이용 내역
- 세금계산서 발행 데이터

## 권한 관리

### 역할 기반 접근 제어 (RBAC)

| 역할 | 대시보드 | 노선 관리 | 데이터 분석 | 정산 관리 | 사용자 관리 |
|-----|---------|---------|-----------|---------|----------|
| KD 운영자 | ✅ 전체 | ✅ 전체 | ✅ 전체 | ✅ 전체 | ✅ |
| 기업 관리자 | ❌ | ❌ | ✅ 소속 기업만 | ✅ 소속 기업만 | ✅ 소속 임직원만 |

## 개발 가이드

### 새로운 페이지 추가
1. `src/pages/` 하위에 페이지 디렉토리 생성
2. 페이지 컴포넌트 작성
3. `src/App.tsx`에 라우트 추가

### API 호출
React Query를 사용하여 API를 호출합니다:

```typescript
import { useQuery } from '@tanstack/react-query';
import { getRoutes } from '@/services/api/routes';

function RouteList() {
  const { data, isLoading, error } = useQuery({
    queryKey: ['routes'],
    queryFn: getRoutes,
  });

  if (isLoading) return <Loading />;
  if (error) return <Error message={error.message} />;

  return <RouteTable data={data} />;
}
```

### 상태 관리
전역 상태는 Redux Toolkit을 사용하고, 서버 상태는 React Query를 사용합니다.

```typescript
// Redux - 클라이언트 상태
import { useAppSelector } from '@/store/hooks';

const user = useAppSelector(state => state.auth.user);

// React Query - 서버 상태
const { data: vehicles } = useQuery(['vehicles'], fetchVehicles);
```

## 테스트

```bash
# 단위 테스트
npm run test

# E2E 테스트 (Playwright)
npm run test:e2e
```

## 배포

### 프로덕션 빌드
```bash
npm run build
```

빌드된 파일은 `dist/` 디렉토리에 생성됩니다.

### Docker
```bash
docker build -t kd-dsp-web-portal .
docker run -p 80:80 kd-dsp-web-portal
```

## 코드 규칙

- ESLint + Prettier 사용
- 컴포넌트는 함수형 컴포넌트 사용
- TypeScript strict 모드 활성화
- CSS-in-JS (Emotion) 사용

## 참고 문서

- [React 공식 문서](https://react.dev/)
- [Material-UI 문서](https://mui.com/)
- [Redux Toolkit 문서](https://redux-toolkit.js.org/)
- [프로젝트 PRD](../docs/PRD.md)
